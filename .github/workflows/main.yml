name: CI-CD Terraform Deploy

on:
  push:
    branches: [ "master" ]

jobs:
  package-and-deploy:
    runs-on: ubuntu-latest
    steps:
      # 1️⃣ Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Debug: list repo files
      - name: Debug repo files
        run: |
          pwd
          ls -R event-driven-pipeline/scripts

      # 3️⃣ Set up Python (for Lambda build)
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      # 4️⃣ Build Lambdas using correct script path
      - name: Build Lambdas
        run: |
          chmod +x event-driven-pipeline/scripts/lambda.sh
          ./event-driven-pipeline/scripts/lambda.sh

      # 5️⃣ Configure AWS Credentials (use GitHub secrets!)
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: AKIARJHKLYNZKZ5DIAED
          aws-secret-access-key: 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
          aws-region: us-east-1
          
      # 6️⃣ Setup Terraform
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: "1.5.0"

      # 7️⃣ Terraform Init
      - name: Terraform Init
        working-directory: terraform
        run: terraform init

      # 8️⃣ Terraform Validate
      - name: Terraform Validate
        working-directory: terraform
        run: terraform validate

      # 9️⃣ Terraform Plan
      - name: Terraform Plan
        working-directory: terraform
        run: terraform plan \
          -var="raw_bucket_name=archana-devops-raw-2025" \
          -var="processed_bucket_name=archana-devops-processed-2025"

      # 🔟 Terraform Apply
      - name: Terraform Apply
        working-directory: terraform
        run: terraform apply -auto-approve \
          -var="raw_bucket_name=archana-devops-raw-2025" \
          -var="processed_bucket_name=archana-devops-processed-2025"
